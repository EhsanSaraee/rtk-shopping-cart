import { Navbar, CartContainer } from 'components';

const App = () => {
   return (
      <main style={{ textAlign: 'center' }}>
         <Navbar />
         <CartContainer />
      </main>
   );
};

export default App;
